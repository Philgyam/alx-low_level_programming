#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: count arguments
 * @argv: name of array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[argc * 0]);

	return (0);
}
