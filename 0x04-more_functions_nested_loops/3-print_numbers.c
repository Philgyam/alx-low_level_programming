#include "main.h"
/**
 * print_numbers- Print everything
 * return : always success
 * void: the empty space;
 */
int print_numbers(void)
{
	int a;
	
	for (a = 0; a <= 9; a++)
	{
		putchar(a + '0');
	}
}
