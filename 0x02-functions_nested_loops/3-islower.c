#include "main.h"
/**
 * Main- Its part
 * c - its the int
 *int _islower - take us through
 *
 * Return: always 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}
