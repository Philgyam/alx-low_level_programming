#include "main.h"
/**
 * _abs - Returns absolute
 * @c: The number
 * Return: Always succes
 */
int _abs(int c)
{

	if (c <= 0)

	{
	int abs_val;

	abs_b = c * -1;
	return (abs_val);
	}
return (c);
}
