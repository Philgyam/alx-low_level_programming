#include "main.h"
/**
 * print_alphabet - its the center
 *
 * Return: void
 */
void print_alphabet(void)
{
	char c;
	int b;
	for (b = 1; b <= 10; b++){

	for (c='a'; c<='z', c++)
	{putchar(c);}
	putchar('\n');
	}
}
