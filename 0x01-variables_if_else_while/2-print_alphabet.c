#include <stdio.h>
#include <ctype.h>
/**
 * main - main function is described here
 * Return: 0 (success)
 */
int main(void)
{
	char a = 'a';

	while (a <= 'z')
	{putchar(a);
	a++; }
	putchar('\n');
	return (0);
}
