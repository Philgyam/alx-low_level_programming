#include <stdlib.h>
#include <stdio.h>

/**
 * main - The main function
 * Return: 0 (succes)
 */
int main(void)
{
	char z = 'z';

	while (z >= 'a')
	{putchar(z);
		z--;
	}
	putchar('\n');
return (0);
}
